{
  "name": "security-triage",
  "private": true,
  "description": "Repository-level Git hooks only; the apps live in frontend/ and backend/.",
  "scripts": {
    "prepare": "husky"
  },
  "engines": {
    "node": ">=22.22.1"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.3",
    "@commitlint/config-conventional": "^21.2.3",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1"
  }
}
