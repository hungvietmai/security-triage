# Optional overrides for local Docker Compose. Copy to .env if needed.
# Defaults are local development credentials, not production secrets.
POSTGRES_PASSWORD=triage_dev
S3_ACCESS_KEY=triage-local
S3_SECRET_KEY=triage-local-development-only
S3_BUCKET=source-artifacts
FRONTEND_PORT=5173
API_PORT=8000
POSTGRES_PORT=5432
S3_PORT=8333
