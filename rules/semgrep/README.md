# Semgrep rules

Add versioned, reviewed rules here when implementing the scanner adapter.
Keep development fixtures separate from the held-out evaluation dataset.
Start with a narrow command-injection scope (CWE-78).
